{
  "name": "reader",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "vite build",
    "build:plugins": "node plugins/build-plugins.js",
    "dev": "vite dev --host",
    "preview": "vite preview",
    "sync": "npm run build && npx cap sync",
    "open:ios": "npx cap open ios",
    "open:android": "npx cap open android",
    "run:ios": "npx cap run ios",
    "run:android": "npx cap run android",
    "build:android": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "build:android-update": "node update-version.js && npm run build && npx cap sync android && cd android && ./gradlew assembleDebug",
    "build:android-dev": "npm run build && npx cap sync android && cd android && ./gradlew assembleDebug -PdevBuild=true",
    "install:android": "npm run build:android && adb install -r android/app/build/outputs/apk/debug/app-debug.apk",
    "run:android-dev": "npm run build && npx cap run android --target=emulator",
    "run:android-device": "npm run build && npx cap run android --target=device",
    "build:android-release": "npm run build && npx cap sync android && cd android && ./gradlew assembleRelease",
    "generate:assets": "npx @capacitor/assets generate",
    "generate:simple-icons": "node generate-simple-icons.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/app": "^7.1.0",
    "@capacitor/cli": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@capacitor/filesystem": "^7.1.4",
    "@capacitor/ios": "^7.4.3",
    "@capacitor/share": "^7.0.1",
    "@capacitor/status-bar": "^7.0.3",
    "@capawesome/capacitor-file-picker": "^7.2.0",
    "capacitor-document-tree-access": "file:./plugins/capacitor-document-tree-access",
    "sql.js": "^1.13.0"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-env": "^7.23.0",
    "@capacitor/assets": "^3.0.5",
    "babel-jest": "^30.0.3",
    "jest": "^30.0.3",
    "jest-environment-jsdom": "^30.0.3",
    "vite": "^7.0.4"
  }
}
